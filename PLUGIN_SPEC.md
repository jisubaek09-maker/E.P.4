# GeoHint 플러그인 (Paper, 마인크래프트 26.2 대상)

## 0. 이 문서가 다루는 것

요청한 10가지를 모두 구현했고, 컴파일 확인 방법과 몇 가지 반드시 확인해야 할 부분을 정리했다.

## 1. 확정한 절대연령 수치 (지난 답변의 "불균등 제안"을 실제 숫자로 확정)

| 층 | 나이(Ma) | 구성원소(가상) | 반감기(Ma) | 경과 반감기 | 모원소:자원소 |
|---|---|---|---|---|---|
| A | 380 | θ(세타) | 95.0 | 4 | 6.25% : 93.75% (1:15) |
| C | 372 | — (화석) | — | — | — |
| F | 366 | λ(람다) | 122.0 | 3 | 12.5% : 87.5% (1:7) |
| D | 348 | ξ(크사이) | 174.0 | 2 | 25% : 75% (1:3) |
| E | 339 | π(파이) | 339.0 | 1 | 50% : 50% (1:1) |
| G | 331 | σ(시그마) | 165.5 | 2 | 25% : 75% (1:3) |
| H | 323 | — (화석) | — | — | — |
| B | 312 | — (소거법) | — | — | — |

C(372)·H(323)는 실제 GSSP 화석 연대 그대로다. 나머지는 "나이 = 반감기 × 경과 횟수"가 정확히 맞도록 역산한 값이라, 학생이 직접 계산해서 검산할 수 있다. **원소 이름을 그리스 문자로 지어낸 이유** — 실제 동위원소 이름(우라늄-납, 포타슘-아르곤 등)을 쓰면서 반감기 수치만 실습용으로 바꾸면, 학생이 나중에 실제 반감기를 찾아봤을 때 "AI가 틀린 과학 정보를 줬다"고 오해할 수 있다. 완전히 가상의 원소로 처리해 그 혼동을 없앴다. 실제 원소명을 쓰고 싶으면 `RockLayer.java`의 `element` 필드만 바꾸면 된다.

지층은 8개(A,C,F,D,E,G,H,B)인데 `/hint`가 되는 층은 5개(A,F,D,E,G)뿐이라, 힌트 6회 제한이면 1회는 여분이다.

## 2. 명령어

| 명령어 | 설명 |
|---|---|
| `/help` | 명령어 목록 |
| `/hint` | "연대를 확인할 암석을 우클릭해주세요" → 들고 있는 표본을 우클릭 → 구성원소/반감기/모원소·자원소 비율 표시 (총 6회) |
| `/answer` | 제출 모드 시작 → 오래된 시료부터 순서대로 우클릭 → 8개 다 맞으면 전체 공지 |
| `/dm <닉네임> <내용>` | 귓속말 |
| `/health` | 체력 회복 |
| `/hungry` | 허기 회복 |
| `/tools` | 효율10·파괴불가 네더라이트 삽/곡괭이 재지급 (처음 접속 시 자동 지급도 됨) |
| `/geo setregion <1-5> <색상>` | (관리자) 서 있는 자리를 그 팀의 발광 표식 위치로 등록 |
| `/geo removeregion <1-5>` / `/geo list` / `/geo reload` | (관리자) |

## 3. 요청 항목별 구현 방식과 주의할 점

**1) /hungry, /health** — 그대로 구현. 누구나 사용 가능(권한 제한 없음).

**2) 5팀 경쟁 + 발광 효과** — 겉면은 말씀하신 대로 테라코타로 직접 다르게 칠하시면 되고, 플러그인은 **각 건축물 위에 투명한 발광 표식**(ArmorStand + Glowing)을 세운다. Glowing 효과는 벽 너머로도 실루엣이 비치기 때문에, 서버 어디서든 "저기 몇 번 팀 건물이다"가 스코어보드 팀 색으로 바로 보인다. 각 팀 건축물의 서남쪽 아래 모서리(스키매틱을 붙여넣은 자리)에 서서 `/geo setregion <번호> <색상>` 한 번씩 실행하면 끝. 색상은 ChatColor 이름(RED, GOLD, YELLOW, GREEN, AQUA, LIGHT_PURPLE, WHITE 등)을 쓴다.

**3) 감마(밝기)** — 여기서 분명히 짚어야 하는 게, **서버는 클라이언트의 감마 슬라이더 자체를 조작할 수 없다.** 그래서 사실상 같은 효과를 내는 표준적인 방법인 **무한 지속 야간투시(파티클·아이콘 없이)** 를 접속 시 자동으로 걸어주는 방식으로 구현했다. 우유를 마셔서 지워지면 30초마다 다시 걸어주는 감시 작업도 넣었다. 진짜 감마 값 자체가 필요하다면 그건 각자 클라이언트 설정(비디오 설정 → 밝기를 최대로)에서 학생들이 직접 조절해야 한다 — 서버 쪽에서는 원천적으로 불가능하다.

**4) 블록 이름** — 지층 블록을 캐면 원래 블록 대신 이름이 바뀐 "지질 시료" 아이템이 나온다 (예: "지질 시료 - 이암(셰일) [A층]"). 나이는 이름에 안 넣었다 — 이름만 보고 순서를 알아버리면 상대연령 추론 자체가 무의미해지기 때문. 대신 층 기호(A~H)와 암석 이름만 보여서, `/hint`나 `/answer` 할 때 "이게 무슨 표본인지"는 알되 "몇 년 전인지"는 몰라야 하는 균형을 맞췄다.

**5) /hint 6회** — 위 1번 표 참고. 5개 층 + 여분 1회.

**6) /help** — 완료.

**7) /dm** — 완료. 콘솔에서도 실행 가능(다른 선생님이 개입할 때 등).

**8) 효율10·파괴불가 네더라이트 삽·곡괭이** — 접속하자마자 자동 1회 지급(재접속해도 중복 지급 안 되도록 처리), `/tools`로 잃어버렸을 때 재지급. "내구성 무한"은 인챈트가 아니라 아이템 자체를 파괴 불가(Unbreakable) 처리한 것이라 진짜로 절대 안 닳는다.

**9) /hint 대화형 흐름** — 정확히 요청하신 대로: `/hint` → "연대를 확인할 암석을 우클릭해주세요." → 표본 우클릭 → 구성원소/반감기/모원소자원소(%) 순서로 채팅 출력. 엉뚱한 아이템(표본이 아니거나 화석층·관입암 표본)을 우클릭하면 "올바른 암석이 아닙니다. 다시 입력해주세요." 하고, 이때는 `/hint`를 다시 입력해야 한다(한 번의 우클릭 시도로 끝나는 방식).

**10) /answer 흐름** — `/hint`와 같은 패턴. 다만 `/answer`는 한 번 켜두면 계속 유지되니까 다시 입력할 필요 없이 표본을 순서대로 계속 우클릭하면 된다. 8개를 다 맞히는 순간 **서버 전체에 방송**된다: "OOO님이 지층 추론에 성공하였습니다!"

## 4. 반드시 확인해야 할 것 — 26.2 API 이름

이 사양은 지식 컷오프(2026년 1월) 이후에 나온 버전이라, 코드에서 다음 두 곳은 **실제 26.2 Paper API 문서를 보고 이름이 맞는지 확인**해야 한다. 이 환경은 인터넷이 막혀 있어서 실제 26.2 API로 컴파일 테스트를 못 해봤다 (Bukkit/Paper 클래스 구조를 최대한 정확히 흉내 낸 자체 스텁으로 문법·로직만 확인했다).

- `pom.xml` 의 `paper-api` 버전(`1.21.4-R0.1-SNAPSHOT`으로 임시 지정) — 26.2에 맞는 실제 좌표로 바꿔야 함. [papermc.io](https://papermc.io) 다운로드 페이지나 `repo.papermc.io` 에서 확인.
- `ToolFactory.java` 의 `Registry.ENCHANTMENT.get(NamespacedKey.minecraft("efficiency"))` — 인챈트 이름 키가 바뀌었을 가능성. 정적 필드(`Enchantment.EFFICIENCY`) 대신 일부러 이름으로 조회해서 위험을 줄여놨다.

## 5. 빌드 방법

인터넷이 되는 컴�터에서:

```
cd geohint-plugin
mvn package
```

`target/geohint-1.0.0.jar` 가 나오면 서버의 `plugins/` 폴더에 넣고 재시작. Maven이 없으면 IntelliJ에 이 폴더를 열어서 Maven 프로젝트로 인식시킨 뒤 `package` 만 눌러도 된다.
